@{ 
    ViewData["Title"] = "DataError";
}

<h1 class="text-danger">Error!</h1>
<h2 class="text-danger">An unexpected error occured during requesting process.</h2>

@if(Model.ShowGetId)
{
    <p>
        <bold>Request the ID:</bold> <code>@Model.GetId</code>
    </p>

}

<h3>More Information</h3>
<p>
    Going to <bold>Development</bold> setting will direct the cause of the error.
</p>
<p>
    <bold>Development settings should not allow for such faulty errors. Cannot be enabled in deployed applications!</bold>
</p>
